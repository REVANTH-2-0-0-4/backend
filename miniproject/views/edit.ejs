<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-900 w-full min-h-screen p-10 text-white font-serif">
      <div class="flex items-center justify-between">
        <a href="/" class="text-blue-600">Back to Home</a>
        <a href="/logout" class="text-red-600">Logout</a>
      </div>

      <div class="form mt-10 mb-2 text-[1.5vw]">Edit  your  post</div>
      <form action="/edit/<%=post._id %>" method="post" class="flex items-center ">
        <input type="text"
        class="w-[40vw] rounded-lg outline-none  bg-zinc-600 p-4"
        value="<%= post.content %>"
        name="content"
        >
        <input
          type="submit"
          class="p-4 bg-blue-700 rounded-lg ml-4"
          value="Edit  post"
        />
      </form>

    </div>
  </body>
</html>
